log2(x) := log(x)/log(2);
assume(a < 1);
assume(a > 0);
h : a*log2(a) + 2*a*log2(2*a)+(1-3*a)*log2(4/(1-3*a));
ratsimp(h);
diff(%,a);
ratsimp(%);
x : num(%);
solve(expand(x), a);

eq : log2(a) + 2*log2(2*a) - 3*log2((1-3*a)/4) = 0;
solve(eq, a);
